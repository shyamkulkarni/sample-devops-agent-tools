# mcp-proxy's own dependency spec allows any mcp>=1.27.1 with no upper
# bound. The MCP Python SDK released a breaking v2.0.0 (new stateless
# 2026-07-28 protocol revision, restructured internal APIs) that
# mcp-proxy 0.12.0 is not compatible with -- it imports request_ctx from
# mcp.server.lowlevel.server, which v2 removed. Pin mcp to the last 1.x
# release to avoid pulling in the incompatible v2 line until mcp-proxy
# publishes a release that supports it.
uv
mcp==1.29.0
mcp-proxy==0.12.0
